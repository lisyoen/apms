import bcrypt from "bcryptjs"; import { db } from "@/lib/db"; import { createSession } from "@/lib/session";
export async function POST(request: Request) { let body: { email?: string; password?: string }; try { body = await request.json(); } catch { return Response.json({ error: "잘못된 요청입니다." }, { status: 400 }); } if (!body.email || !body.password) return Response.json({ error: "이메일과 비밀번호를 입력하세요." }, { status: 400 }); const result = await db.query("SELECT email, password_hash, role FROM users WHERE lower(email) = lower($1)", [body.email]); const user = result.rows[0]; if (!user || !(await bcrypt.compare(body.password, user.password_hash))) return Response.json({ error: "이메일 또는 비밀번호가 올바르지 않습니다." }, { status: 401 }); const token = await createSession({ email: user.email, role: user.role }); const response = Response.json({ ok: true }); response.headers.append("Set-Cookie", `apms_session=${token}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=86400`); return response; }
