import { apiError,requireUser } from "@/lib/auth";import {createSession,sessionTtlSeconds} from "@/lib/session";
export async function GET(){try{const u=await requireUser();const response=Response.json({id:u.id,email:u.email,display_name:u.display_name,role:u.role});const token=await createSession({email:u.email,role:u.role});response.headers.append("Set-Cookie",`dirigo_session=${token}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=${sessionTtlSeconds()}`);return response}catch(e){return apiError(e)}}
