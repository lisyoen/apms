import { ownedProject, requestJson, jsonError } from "@/lib/api"; import { DOC_KINDS, readDocument, writeDocument, type DocKind } from "@/lib/storage";
export async function GET(_: Request, { params }: RouteContext<"/api/projects/[slug]/docs/[kind]">) { const {slug,kind}=await params; const own=await ownedProject(slug); if("error" in own)return own.error; if(!DOC_KINDS.includes(kind as DocKind))return jsonError("invalid_kind",404); const content=await readDocument(own.user.storageSlug,slug,kind as DocKind); return new Response(content,{headers:{"Content-Type":"text/markdown; charset=utf-8"}}); }
export async function PUT(request: Request, { params }: RouteContext<"/api/projects/[slug]/docs/[kind]">) { const {slug,kind}=await params; const own=await ownedProject(slug); if("error" in own)return own.error; if(!DOC_KINDS.includes(kind as DocKind))return jsonError("invalid_kind",404); const type=request.headers.get("content-type")||""; const content=type.includes("application/json")?(await requestJson<{content?:string}>(request))?.content:await request.text(); if(typeof content!=="string")return jsonError("content_required"); await writeDocument(own.user.storageSlug,slug,kind as DocKind,content); return Response.json({ok:true}); }
