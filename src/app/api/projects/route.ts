import { apiError,requireUser } from "@/lib/auth";import { db } from "@/lib/db";import{initializeProject,slugify,userSlug}from"@/lib/storage";
export async function GET(){try{const u=await requireUser();return Response.json({items:(await db.query(`SELECT p.id,p.name,p.slug,p.view_mode,p.updated_at,json_build_object('pending',count(t.*) FILTER(WHERE t.status='pending'),'in_progress',count(t.*) FILTER(WHERE t.status='in-progress'),'done',count(t.*) FILTER(WHERE t.status='done'),'failed',count(t.*) FILTER(WHERE t.status='failed')) task_counts FROM projects p LEFT JOIN tasks t ON t.project_id=p.id WHERE p.owner_id=$1 AND p.archived_at IS NULL GROUP BY p.id ORDER BY p.updated_at DESC`,[u.id])).rows})}catch(e){return apiError(e)}}
export async function POST(request:Request){try{const u=await requireUser();const body=await request.json();if(!body?.name?.trim())return Response.json({error:"name_required"},{status:400});const slug=slugify(body.slug||body.name);let project;try{project=(await db.query("INSERT INTO projects(owner_id,name,slug) VALUES($1,$2,$3) RETURNING id,name,slug",[u.id,body.name.trim(),slug])).rows[0]}catch(e){if((e as{code?:string}).code==="23505")return Response.json({error:"slug_exists"},{status:409});throw e}try{await initializeProject(userSlug(u.email,u.id),slug,body.name.trim())}catch(e){await db.query("DELETE FROM projects WHERE id=$1",[project.id]);throw e}return Response.json({...project,documents_initialized:true},{status:201})}catch(e){return apiError(e)}}
