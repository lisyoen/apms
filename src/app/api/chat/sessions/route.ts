import { apiError,requireUser } from "@/lib/auth";import { db } from "@/lib/db";
export async function GET(){try{const u=await requireUser();const r=await db.query(`SELECT s.*,p.name project_name,p.slug project_slug FROM sessions s LEFT JOIN projects p ON p.id=s.project_id WHERE s.user_id=$1 ORDER BY s.created_at DESC`,[u.id]);return Response.json({items:r.rows})}catch(e){return apiError(e)}}
export async function POST(req:Request){try{const u=await requireUser();const b=await req.json();let limit=100000;if(b.project_id){const own=await db.query("SELECT id FROM projects WHERE id=$1 AND owner_id=$2",[b.project_id,u.id]);if(!own.rowCount)return Response.json({error:"프로젝트를 찾을 수 없습니다."},{status:404})}const c=await db.query("SELECT context_window FROM llm_connections WHERE enabled AND is_default ORDER BY updated_at DESC LIMIT 1");if(c.rows[0])limit=c.rows[0].context_window;const r=await db.query("INSERT INTO sessions(user_id,project_id,title,context_limit) VALUES($1,$2,$3,$4) RETURNING *",[u.id,b.project_id||null,String(b.title||"새 대화").slice(0,120),limit]);return Response.json(r.rows[0],{status:201})}catch(e){return apiError(e)}}
