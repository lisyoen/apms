"use client";
import { KeyboardEvent, useCallback, useEffect, useState } from "react";
import { MarkdownContent } from "@/components/MdViewer";
export type SaveResult={ok:true;content:string;etag:string;updatedAt:string}|{ok:false;conflict?:boolean;message:string};
export default function MdEditor({title,initialContent,onSave,onCancel,onReload}:{title:string;initialContent:string;onSave:(content:string)=>Promise<SaveResult>;onCancel:()=>void;onReload:()=>Promise<void>}){const[content,setContent]=useState(initialContent),[split,setSplit]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[conflict,setConflict]=useState(false);const changed=content!==initialContent;const save=useCallback(async()=>{if(!changed||saving)return;setSaving(true);setError("");const result=await onSave(content);setSaving(false);if(!result.ok){setError(result.message);setConflict(!!result.conflict)}},[changed,saving,onSave,content]);useEffect(()=>{const beforeUnload=(event:BeforeUnloadEvent)=>{if(!changed)return;event.preventDefault();event.returnValue=""};window.addEventListener("beforeunload",beforeUnload);return()=>window.removeEventListener("beforeunload",beforeUnload)},[changed]);useEffect(()=>{const key=(event:globalThis.KeyboardEvent)=>{if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="s"){event.preventDefault();void save()}};window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)},[save]);useEffect(()=>{const leave=(event:MouseEvent)=>{if(!changed)return;const target=event.target as HTMLElement;if(!target.closest(".project-sidebar a,.project-sidebar button"))return;if(!window.confirm("저장하지 않은 변경 사항을 버리고 이동할까요?")){event.preventDefault();event.stopPropagation()}};document.addEventListener("click",leave,true);return()=>document.removeEventListener("click",leave,true)},[changed]);function cancel(){if(!changed||window.confirm("저장하지 않은 변경 사항을 버릴까요?"))onCancel()}function tab(event:KeyboardEvent<HTMLTextAreaElement>){if(event.key!=="Tab")return;event.preventDefault();const el=event.currentTarget;const next=`${content.slice(0,el.selectionStart)}  ${content.slice(el.selectionEnd)}`;const cursor=el.selectionStart+2;setContent(next);requestAnimationFrame(()=>el.setSelectionRange(cursor,cursor))}return <section className="md-editor" data-testid="md-editor"><header><strong>{title}</strong><div><button type="button" onClick={()=>setSplit(v=>!v)}>{split?"미리보기 숨기기":"분할 미리보기"}</button><button type="button" onClick={cancel}>취소</button><button type="button" className="primary" disabled={!changed||saving} onClick={()=>void save()}>{saving?"저장 중…":"저장"}</button></div></header>{error&&<div className="editor-error" role="alert">{error}{conflict&&<div><button type="button" onClick={()=>void onReload()}>다시 불러오기</button><button type="button" onClick={()=>setConflict(false)}>내 내용 유지</button></div>}</div>}<div className={split?"md-editor-grid":"md-editor-grid preview-hidden"}><textarea aria-label="Markdown 편집" value={content} onChange={e=>setContent(e.target.value)} onKeyDown={tab}/>{split&&<div className="md-editor-preview" aria-label="Markdown 미리보기"><MarkdownContent content={content}/></div>}</div></section>}
