APMS 기획 inbox (planning/additional.txt)

이 파일은 사용자와 Claude Chat 이 기획 지시/답변을 주고받던 기존 inbox 입니다. 2026-09-11 부터 신규 기획 입력은 날짜별 GitHub issue 중심 운영으로 이관 예정입니다.
- 사용자: 새 기획 항목이나 질문에 대한 답변은 업무 클로가 당일 `기획 YYYY-MM-DD` GitHub issue 로 정리하는 방식을 기본으로 합니다. 당일 이슈가 이미 있으면 이슈 본문은 건드리지 않고 댓글로 추가 기획을 남깁니다.
- Claude Chat: 클로드큐 프로젝트 작업지침에 따라 당일 기획 issue 를 읽고, 처리 결과와 질문을 같은 issue 댓글로 남기는 방식으로 전환 예정입니다.
- 이 파일: 기존 항목 정리와 이관 전 과도기 보조 용도로만 사용합니다. 아래 전환 지시문을 클로드큐 작업지침에 반영한 뒤에는 새 기획을 이 파일에 계속 누적하지 않습니다.
- 업무 클로: 사용자가 업무 클로에서 설명하는 APMS 기획은 직접 구현/배포하지 않고, 당일 기획 issue 에 입력으로 정리합니다. 사용자가 명시적으로 "직접 작업", "코드 수정", "배포"를 지시한 경우에만 구현 작업으로 전환합니다.
- 공개 저장소이므로 시크릿·토큰·내부 IP·개인 경로는 적지 않습니다.

## Claude → 사용자 (답변 요청)

1. 항목 3(두 번째 질문 "요청을 처리하지 못했습니다") 은 commit e91dadf (2026-09-11 10:29) 로 이미 수정·배포된 것으로 확인했습니다. test2 세션에서 두 번째 질문을 다시 보내 재현되지 않으면 이 항목은 삭제해 주시고, 재현되면 증상을 적어 주세요.
2. 솔루션 명칭 후보(구글독스 "APMS 솔루션 명칭 후보 (2026-09-11)", 1순위 Dirigo) 중 확정 명칭과 repo 명을 적어 주시면 README 태그라인·package.json·repo rename·화면 로고 반영 지시서를 발주합니다.


## 사용자 → Claude (신규 기획 항목)

(여기에 적어주시면 다음 세션 진입 시 자동 처리합니다)

2026-09-11 10:58 사용자 입력 — 기획 입력 방식 전환 및 Dirigo 명칭 확정

1. 프로젝트 작업 방식 변경
   - 현재 방식:
     - `planning/additional.txt`에 기획을 남기면 클로드큐가 이를 읽고 작업지시서를 연속 발주한다.
     - 클로드큐는 처리 이력을 `planning/history.md`와 오늘자 `기획 YYYY-MM-DD` 이슈에 기록한다.
   - 앞으로의 방식:
     - 오늘자 `기획 YYYY-MM-DD` 이슈에 기획을 남기면 클로드큐가 이를 읽고 작업지시서를 연속 발주한다.
     - 클로드큐는 반영상태와 결과를 해당 이슈의 댓글로 업데이트한다.
     - 이슈 본문은 건드리지 않는다.
     - 이후 작업은 댓글로 추가 기획과 결과를 주고받는 방식으로 운영한다.
   - 클로드큐 APMS 프로젝트 작업지침에 위 운영 방식을 반영한다.

2. 프로젝트명/저장소명 변경
   - 클로드큐 프로젝트명과 GitHub repo 이름을 `dirigo`로 변경한다.
   - 한국어 표기는 `디리고`로 사용한다.
   - 저장소명 제안: `lisyoen/dirigo`
   - 도메인 후보: `dirigo.craftbay.io`
   - CLI 후보: `dirigo run`
   - 패키지명 후보: `dirigo`

3. 명칭 지칭 규칙
   - 앞으로 `apms`, `APMS`, `dirigo`, `디리고`라는 표현은 모두 새로운 프로젝트 `Dirigo(디리고)`를 지칭한다.
   - 기존 APMS 명칭은 과거 명칭/전환 전 명칭으로만 취급한다.
   - 기존 파일명, 경로, 이슈, history에서 `apms`가 남아 있으면 동일 프로젝트의 이전 명칭으로 해석한다.
   - 신규 문서, README, 작업지시서, 이슈, 화면 문구, CLI, 패키지명, 저장소명은 가능한 한 `Dirigo`/`dirigo` 기준으로 작성한다.

4. README 반영 내용
   - GitHub `README.md`에 아래 취지의 명칭 설명과 태그라인을 추가한다.
   - 명칭: `Dirigo (디리고)`
   - 어원:
     - 라틴어 `dirigere`에서 온 말로, "똑바로 이끌다", "방향을 잡다"라는 뜻이다.
     - `Dirigo`는 1인칭 현재형으로 "나는 이끈다"라는 의미를 가진다.
     - 영어 `direct`, `director`, 독일어 `Dirigent`, 스페인어 `dirigir`와 같은 어근이다.
     - 미국 메인주의 주 표어 "I lead"로도 쓰인다.
   - 추천 사유:
     - "AI가 프로젝트를 지휘한다"는 의미를 한 단어로 압축한다.
     - `director`와 `conductor`의 뉘앙스가 같은 어근 안에 들어 있다.
     - 3음절이라 한국어로 "디리고"라고 부르기 쉽고, 영어권에서도 그대로 읽기 쉽다.
     - GitHub 이름 충돌이 적고 같은 이름의 유명 소프트웨어가 사실상 없다.
     - 도메인, CLI, 패키지명으로 바로 확장하기 좋다.
   - 태그라인 후보: `Dirigo — AI-directed project management`

(2026-09-11 10:40 반영 완료 — 이슈 #3, #026 응답 생성 중 입력창 활성 / #027 세션 제목 자동 요약(hold) / 항목 3 은 e91dadf 로 수정 완료 / #021 자동 로그인 설계 개정. 원문은 planning/history.md)
(2026-09-10 17:05 반영 완료 — 이슈 #2 코멘트, #022 새 작업 모달 / #023 챗봇 가로 스크롤 버그 / #024 문서 저장 충돌·사이드바 스크롤 버그 / #025 setting.md 기본값. 원문은 planning/history.md)
