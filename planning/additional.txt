APMS 기획 inbox (planning/additional.txt)

이 파일은 사용자와 Claude Chat 이 기획 지시/답변을 주고받는 inbox 입니다. 2026-09-09 부터 이 파일이 유일한 기획 입력 위치입니다.
- 사용자: 새 기획 항목이나 질문에 대한 답변을 "사용자 → Claude" 절 아래에 적고 commit/push 합니다.
- Claude Chat: 세션 시작 시 git pull 후 이 파일을 먼저 읽고, 반영한 항목은 삭제하며, 물어볼 사항은 "Claude → 사용자" 절에 남기고 commit/push 합니다.
- 공개 저장소이므로 시크릿·토큰·내부 IP·개인 경로는 적지 않습니다.

## Claude → 사용자 (답변 요청)

1. PoC 전용 OpenAI API 키: 발급 후 채팅으로 전달 (Claude Chat 이 시크릿 저장소에 보관 → 기본 LLM 연결 교체).
2. 완료 이메일용 Gmail 앱 비밀번호: 시크릿 저장소에 저장 요청 (형식은 Claude Chat 이 안내한 대로).
3. #009 실행 시 OpenCode ChatGPT OAuth 로그인 1회가 필요할 수 있음 — 워커 보고서 절차 확인 후 안내 예정.
4. #013(원격 워크스페이스) 는 아래 가정으로 발주함. 이의가 있으면 "사용자 → Claude" 절에 답변:
   - 실행 모드 B-2(워커를 개발PC 에서 SSH 로 실행) 기본
   - 1차 OS: Windows(PowerShell + OpenSSH Server) 우선, Linux/mac 동일 인터페이스
   - SSH 자격: APMS 가 사용자별 키 발급 → 사용자가 개발PC authorized_keys 등록
   - PoC 검증 대상: 사용자 집PC(Windows). 접속 정보는 시크릿 저장소에 별도 등록(공개 문서 기재 금지), 집PC 에 OpenSSH Server·OpenCode 설치·OAuth 로그인 준비 필요

(2026-09-09 23:15 반영 완료 — 챗봇 우측 패널 #012 발주, 원격 워크스페이스 #013 스테이징, 이슈 #1 코멘트)

## 사용자 → Claude (신규 기획 항목)

(여기에 적어주시면 다음 "기획 반영" 시 처리합니다)
