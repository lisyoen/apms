APMS 기획 inbox (planning/additional.txt)

이 파일은 사용자와 Claude Chat 이 기획 지시/답변을 주고받는 inbox 입니다. 2026-09-09 부터 이 파일이 유일한 기획 입력 위치입니다.
- 사용자: 새 기획 항목이나 질문에 대한 답변을 "사용자 → Claude" 절 아래에 적고 commit/push 합니다.
- Claude Chat: 세션 시작 시 git pull 후 이 파일을 먼저 읽고, 반영한 항목은 `planning/history.md` 로 옮기며(원문 보존), 물어볼 사항은 "Claude → 사용자" 절에 남기고 commit/push 합니다.
- 공개 저장소이므로 시크릿·토큰·내부 IP·개인 경로는 적지 않습니다.

## Claude → 사용자 (답변 요청)

1. #009 실행 시 OpenCode ChatGPT OAuth 로그인 1회(서버 및 개발PC 각각)가 필요할 수 있음 — 워커 보고서 절차 확인 후 안내 예정.
답변> 009 끝난지 오랜데 이 답변 요청이 왜 아직 남아있는지 모르겠음. 조치된 답변 요청은 삭제할 것
2. (해결) 2026-09-10 12:55 재부팅 후 Gemma4 업스트림 정상(모델 목록 응답 확인). 챗봇 UX 테스트 가능.
3. #021 "로그인 정보 저장" 설계를 다음과 같이 확정해 발주 예정: 이메일만 브라우저에 저장 + 체크 시 로그인 유지 30일(미체크 12시간), 비밀번호는 저장하지 않음. 다르게 원하면 여기에 답변.
답변> 체크시 브라우저에 이메일, 비밀번호저장하고 로그인 세션 30일 유지. 자동로그인 체크 안해도 로그인 세션은 24시간 유지


## 사용자 → Claude (신규 기획 항목)

(여기에 적어주시면 다음 세션 진입 시 자동 처리합니다)

2026-09-11 10:25 사용자 입력

1. 새 채팅을 만든 뒤 첫 요청을 보내면, 첫 요청의 핵심 내용을 짧은 문장으로 요약해 대화 제목으로 사용한다.
   - 단순히 원문 앞부분을 잘라 쓰는 것이 아니라 세션 목록에서 구분 가능한 짧은 제목이어야 한다.
   - 첫 요청이 긴 기획/작업 지시여도 제목은 한 줄 요약으로 유지한다.
2. 대화 응답 생성 중에도 대화 입력창은 계속 입력 가능해야 한다.
   - 답변을 기다리는 동안 다음 질문이나 보강 내용을 미리 타이핑할 수 있어야 한다.
   - 전송 동작은 중복 요청 방지를 위해 별도 큐 설계가 확정되기 전까지 막아도 되지만, 작성 중인 draft는 사라지면 안 된다.
3. `test2` 새 대화에서 두 번째 질문을 보낼 때 "요청을 처리하지 못했습니다"가 표시되고 답변이 없는 문제를 분석하고 수정한다.
   - 2026-09-11 10:22~10:24 KST 운영 로그상 `/api/chat` 처리 중 `invalid_doc_kind` 예외가 발생했다.
   - 모델이 `read_doc`/`update_doc` 도구에 허용되지 않은 문서 종류를 넣어도 서버 500으로 대화를 끊지 말고, 허용 문서 종류를 명시한 도구 오류 결과로 처리해 사용자가 답변을 받아야 한다.

(2026-09-10 17:05 반영 완료 — 이슈 #2 코멘트, #022 새 작업 모달 / #023 챗봇 가로 스크롤 버그 / #024 문서 저장 충돌·사이드바 스크롤 버그 / #025 setting.md 기본값. 원문은 planning/history.md)
