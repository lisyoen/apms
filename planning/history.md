# APMS 기획 반영 이력 (planning/history.md)

`planning/additional.txt`(inbox) 에서 반영·발주가 끝난 기획 항목을 날짜별로 옮겨 보관합니다. inbox 에는 미반영 항목과 질문만 남습니다. 확정 설계는 GitHub planning 이슈와 프로젝트 기획서에 있습니다.

## 2026-09-11 10:40 반영 — 이슈 #3, #026 입력창 활성 유지 / #027 세션 제목 자동 요약(hold) / 항목 3 은 e91dadf 로 수정 완료 / #021 자동 로그인 설계 개정

2026-09-11 10:25 사용자 입력

1. 새 채팅을 만든 뒤 첫 요청을 보내면, 첫 요청의 핵심 내용을 짧은 문장으로 요약해 대화 제목으로 사용한다.
   - 단순히 원문 앞부분을 잘라 쓰는 것이 아니라 세션 목록에서 구분 가능한 짧은 제목이어야 한다.
   - 첫 요청이 긴 기획/작업 지시여도 제목은 한 줄 요약으로 유지한다.
2. 대화 응답 생성 중에도 대화 입력창은 계속 입력 가능해야 한다.
   - 답변을 기다리는 동안 다음 질문이나 보강 내용을 미리 타이핑할 수 있어야 한다.
   - 전송 동작은 중복 요청 방지를 위해 별도 큐 설계가 확정되기 전까지 막아도 되지만, 작성 중인 draft는 사라지면 안 된다.
3. `test2` 새 대화에서 두 번째 질문을 보낼 때 "요청을 처리하지 못했습니다"가 표시되고 답변이 없는 문제를 분석하고 수정한다.
   - 2026-09-11 10:22~10:24 KST 운영 로그상 `/api/chat` 처리 중 `invalid_doc_kind` 예외가 발생했다.
   - 모델이 `read_doc`/`update_doc` 도구에 허용되지 않은 문서 종류를 넣어도 서버 500으로 대화를 끊지 말고, 허용 문서 종류를 명시한 도구 오류 결과로 처리해 사용자가 답변을 받아야 한다.

답변 반영 (Claude → 사용자 절):
- 답변> 009 끝난지 오랜데 이 답변 요청이 왜 아직 남아있는지 모르겠음. 조치된 답변 요청은 삭제할 것
- 답변> 체크시 브라우저에 이메일, 비밀번호저장하고 로그인 세션 30일 유지. 자동로그인 체크 안해도 로그인 세션은 24시간 유지

## 2026-09-10 17:05 반영 — 이슈 #2 코멘트, #022 새 작업 모달 / #023 챗봇 가로 스크롤 / #024 문서 저장 충돌·사이드바 스크롤 / #025 setting.md 기본값

(2026-09-10 16:24 신규 — APMS 작업/문서 화면 버그 및 UX 개선)

우선순위 높음:

1. 작업 섹션 하단의 "새 작업지시서" 작성 섹션을 제거한다.
   - 작업 목록 하단에 상시 노출되는 작성 섹션은 화면을 길게 만들고 상태 확인을 방해한다.
   - 대신 "새 작업" 버튼을 누르면 작업지시서 작성 폼이 팝업/모달로 열리게 수정한다.
   - 모달에는 제목, 작업지시서 본문, pre-task/next-task, timeout 등 기존 작성 기능에 필요한 필드를 유지한다.
   - 저장/생성 성공 후에는 모달을 닫고 해당 작업 섹션을 갱신한다.

2. 챗봇을 펼쳤을 때 화면 폭을 넘어가 좌우 스크롤이 생기는 레이아웃 버그를 수정한다.
   - 전체화면/패널 전환 후에도 채팅 영역은 viewport 폭 안에 고정돼야 한다.
   - 긴 코드블록, 긴 URL, 긴 단어가 있어도 페이지 전체에 horizontal scroll이 생기지 않게 한다.
   - 필요한 경우 메시지 내부에서만 wrap 또는 내부 스크롤을 적용한다.

3. `test2.guide.md` 파일 수정 후 저장 시 "다른 곳에서 문서가 수정되어 저장할 수 없다"는 충돌 오류가 뜨는 문제를 버그로 보고 수정한다.
   - 사용자가 방금 연 문서를 수정해 저장했는데 외부 수정이 없었다면 저장돼야 한다.
   - revision/updated_at/If-Match 처리, WYSIWYG 변환 후 저장 전후 revision 갱신, 문서 로딩 직후 ETag 보관 로직을 확인한다.
   - 실제 재현 케이스는 `test2.guide.md` 편집 → 저장이며, false-positive 충돌을 제거한다.

4. md viewer를 스크롤하면 맨 왼쪽 사이드바가 같이 스크롤되는 버그를 수정한다.
   - 문서 본문 스크롤과 좌측 프로젝트/파일 사이드바 스크롤을 분리한다.
   - 사이드바는 viewport 안에서 고정 또는 독립 스크롤되어야 하고, 본문을 아래로 내려도 같이 밀려 내려가면 안 된다.

5. 프로젝트 설정 파일 `.setting.md`가 제목만 있고 비어 있는 문제를 수정한다.
   - 프로젝트 생성 시 기본 설정값이 들어간 `.setting.md`를 생성해야 한다.
   - 클로드큐 프로젝트 설정 파일의 기본 항목을 참고하되, APMS 공개 저장소에 들어가도 되는 일반 설정값만 사용한다.
   - 최소 포함 후보: 프로젝트 목적/운영 방식, 작업지시서 작성 기준, 워커 실행 기준, 보고서 위치/형식, 금지 사항, 공개 저장소 주의사항.
   - 기존 프로젝트의 비어 있는 `.setting.md`를 마이그레이션/보정할 방법도 함께 검토한다.

## 2026-09-10 15:45 반영 — 이슈 #2 `기획 2026-09-10`, #019 채팅 스크롤·복사 / #020 작업 섹션형 / #021 로그인 정보 저장(hold)

(2026-09-10 15:38 신규 — APMS 채팅/작업 화면 사용성 개선)
1. 로그인 화면 "로그인 정보 저장" 체크박스 — 비밀번호 평문 저장 금지, 기존 세션 구조와 충돌 없이 (→ #021: 이메일만 로컬 저장 + 로그인 유지 30일)
2. 채팅 전체화면의 "패널로" 버튼 상단 고정 (→ #019)
3. 위로 스크롤 시 자동 스크롤 중단 + "최신으로" 화살표 버튼 (→ #019)
4. 요청 문구·응답 메시지 단위 복사 버튼, Markdown 원문 복사 (→ #019)
5. 작업 메뉴를 진행→대기→완료→실패→리포트 섹션형으로, 진행 전체·나머지 5개+페이징, 접힘/배지/빈 상태 (→ #020)

## 2026-09-10 12:05 반영 — #017 메시지 시각·LLM 연결 상태, #018 Tiptap WYSIWYG

## 2026-09-09 반영 — 이슈 #1 `기획 2026-09-09`, #001~#016 (상세는 이슈 #1 과 기획서 "2026-09-09 설계 검토 결정" 표)
